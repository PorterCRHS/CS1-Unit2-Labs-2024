a, b, c = 6, 5, - 6

#code each of the expressions from a to f
#make each one saved to a variable

#expression a
expA = a * b + c
print(str(a) + " * " + str(b) + " + " + str(c) + " = " + str(expA)) #should be 24
#code the rest of a to f








# code h-k as functions
#h should be named pythag
#i should be named discriminant
#j should be named quadPlus
#k should be named quadMinus
#call each of the functions once to test them out
def pythag(a,b):
  return (a**2 + b**2)**0.5
